# Copy this file to `.env` and fill in the real values.
# `.env` is gitignored — never commit it.

# API key for the LSE vault (intraday 1-minute bars, splits & dividends).
# Used by projekat.data.vault and projekat.data.corporate via python-dotenv.
LSE_KEY=lse_live_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
